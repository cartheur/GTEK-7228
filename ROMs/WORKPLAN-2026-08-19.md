# ROM Workplan - 2026-08-19

This note captures the current plan for using the `GTEK 7228` to prepare ROM images for the M6x09-II SBC sister project.

## Current Bench Status

The `GTEK 7228` is working well enough to:

- power up reliably
- communicate over serial
- reach the `xxxx>` prompt
- return the `EPROM SELECTION MENU`
- read complete `27128` dumps when recording is enabled

For the current `MODEL 7228 V7.07` unit, the repeatable cold-start serial baseline is:

- `2400`
- `8n1`
- `xonxoff`

## Goal

Prepare burnable `27128` ROM images for the M6x09-II SBC using sources from:

- `/home/cartheur/ame/aiventure/aiventure-github/cartheur/M6x09-II-SBC/src/m6809/`

## ROM Candidates

### First choice: `ASSIST09`

Why this is the preferred first burn:

- it is the cleanest monitor/bootstrap choice
- it is explicitly described in the sister repo as the best ROM starting point
- it should give the SBC a serial monitor and recovery/debug path

Observed source facts:

- `assist09.bin` size: `2048` bytes
- `ROMBEG EQU $F800`
- source adapted for the SBC's `6850` ACIA

### Second choice: `forth09`

Why this is a follow-on experiment instead of the first burn:

- it is plausible for the target hardware
- it assumes a more specific memory map
- it is larger and more environment-specific than the monitor ROM

Observed source facts:

- `forth09.bin` size: `5711` bytes
- source assembled from `ORG $E000`
- source comments describe an `8 KB` ROM region from `E000-FFFF`

## Working Assumption For 27128 Images

Current image-building assumption:

- EPROM offset `0x0000` maps to CPU address `0xC000`
- EPROM offset `0x3FFF` maps to CPU address `0xFFFF`

That means:

- `ASSIST09` at CPU `0xF800` goes at EPROM offset `0x3800`
- `forth09` at CPU `0xE000` goes at EPROM offset `0x2000`

This is plausible and is supported by the sister repo PCB/BOM notes showing `U2` as `27C128` and the ROM select path labeled `/TOP16K`, but it still remains a placement assumption until proven on the live board.

## Combined Image Observation

It is not safe to assume that `ASSIST09` and `forth09` can simply be packed into one `27128` image.

Why:

- `ASSIST09` is assembled for `0xF800-0xFFFF`
- `forth09` is assembled from `0xE000`
- those two placements overlap in the same top ROM area

Practical consequence:

- an `ASSIST09`-only burn is plausible
- a `forth09`-only burn is plausible
- a combined `ASSIST09 + forth09` image would require relocation or a deliberate custom layout, not a simple merge

See also:

- [docs/27128-image-layout.md](/home/cartheur/ame/aiventure/aiventure-github/cartheur/GTEK-7228/docs/27128-image-layout.md)

## Output Folder

Build outputs for this ROM work should go in:

- [ROMs/m6x09-ii-sbc-27128](/home/cartheur/ame/aiventure/aiventure-github/cartheur/GTEK-7228/ROMs/m6x09-ii-sbc-27128)

Expected artifact types:

- padded `27128` binary image
- Intel HEX version of the same image
- metadata text describing placement assumptions
- readback captures after programming

## Suggested Execution Order

1. Build a padded `27128` image for `ASSIST09`.
2. Inspect the output metadata and placement assumptions.
3. Use the apparently blank `27128-yellow` as the first burn candidate.
4. Burn `ASSIST09` first, not `forth09`.
5. Read the programmed EPROM back with the `7228`.
6. Compare readback against the source image.
7. Only after a successful `ASSIST09` boot attempt, consider burning a separate `forth09` ROM.

## Burn Prep For `27128-yellow`

Current assumption:

- [ROMs/27128/27128-yellow](/home/cartheur/ame/aiventure/aiventure-github/cartheur/GTEK-7228/ROMs/27128/27128-yellow) is effectively blank for reuse because the previous read showed only a handful of non-`FF` bytes and no meaningful code pattern

Prepared burn image:

- [ROMs/m6x09-ii-sbc-27128/assist09-27128.bin](/home/cartheur/ame/aiventure/aiventure-github/cartheur/GTEK-7228/ROMs/m6x09-ii-sbc-27128/assist09-27128.bin)
- [ROMs/m6x09-ii-sbc-27128/assist09-27128.hex](/home/cartheur/ame/aiventure/aiventure-github/cartheur/GTEK-7228/ROMs/m6x09-ii-sbc-27128/assist09-27128.hex)
- [ROMs/m6x09-ii-sbc-27128/assist09-27128.txt](/home/cartheur/ame/aiventure/aiventure-github/cartheur/GTEK-7228/ROMs/m6x09-ii-sbc-27128/assist09-27128.txt)

Recommended immediate bench flow:

1. Insert `27128-yellow` and select the matching `27128` device entry on the `7228`.
2. Blank-check or re-read if the programmer workflow makes that easy.
3. Program from the padded `ASSIST09` image.
4. Verify by readback capture with terminal recording enabled.

## Transport Findings - 2026-08-19

The most important confirmed observations from today's programming tests are:

- a single Intel HEX data record at `0x0000` can be programmed successfully
- a single Intel HEX data record at `0x0010` can also be programmed successfully
- the corresponding readbacks were confirmed with:
  - `R0,0F -> 000102030405060708090A0B0C0D0E0F`
  - `R10,1F -> F0F1F2F3F4F5F6F7F8F9FAFBFCFDFEFF`
- a combined two-record Intel HEX session still fails when the second record starts at `0x0010`
- the resulting programmer error is typically `*DT ERR @ 0010`

Interpretation:

- actual EPROM programming is working
- the unresolved problem is the continued Intel HEX session state between records, not basic one-record programming

Additional confirmed clue:

- a previous `*ST ERR @ 0005` on a distinct data record showed that FIFO overflow can occur if the sender outruns the programmer during real programming

Current sender direction:

- the Python transport helper in `scripts/gtek-send-intel-hex.py` is being tuned toward:
  - per-character pacing
  - device-feedback-aware waiting
  - record completion based on idle-without-error rather than requiring a perfect full echo

## Safety Notes

- Do not burn directly from the small raw `.bin` payloads without placement review.
- Always record full ROM reads using the Tcl terminal recording feature.
- Use `scripts/check-intel-hex-capture.py` to verify readback completeness.
- Treat `ASSIST09` as the safer recovery ROM and `forth09` as the more speculative image.
